package io.mec.edge.controller.loadintelligence;

/**
 * Classifies a circuit as critical (never shed) or non-critical (eligible for load shedding).
 */
public enum CircuitClassification {
	CRITICAL,
	NON_CRITICAL
}
